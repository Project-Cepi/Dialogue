{
  "entrypoint": "${project.group}.${project.mainClass}",
  "name": "${project.name}",
  "version": "${project.version}",
  "dependencies": [
    "KStom",
    "Scriptable"
  ],
  "meta": {
    "downloadURL": "https://github.com/Project-Cepi/Dialogue/releases/download/latest/Dialogue-1.0.0-all.jar"
  }
}